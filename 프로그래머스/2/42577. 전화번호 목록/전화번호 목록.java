import java.util.*;
import java.io.*;

class Solution {
    public boolean solution(String[] phone_book) {
        List<String> list = new ArrayList<>(phone_book);
        System.out.println(list);
        
        boolean answer = true;
        return answer;
    }
}